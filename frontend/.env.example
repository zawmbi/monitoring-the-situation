# Frontend env vars (Vite injects VITE_* into import.meta.env)
# In dev, leave these empty/commented — the Vite proxy handles /api/* forwarding.
# Only set for production builds where no proxy is available.
# VITE_API_URL=http://localhost:4100
# VITE_WS_URL=ws://localhost:4100/ws
# VITE_STOCKS_API_URL=http://localhost:4100/api/stocks
# VITE_FLIGHTS_API_URL=http://localhost:4100/api/flights
